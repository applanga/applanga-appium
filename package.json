{
  "name": "applanga-appium",
  "version": "0.0.2",
  "description": "",
  "main": "applanga-appium.js",
  "scripts": {
    "prepublishOnly": "if [ -n \"$(git status --porcelain=v2)\" ]; then echo 'Error: All changes have to be pushed to remote' && exit 1; fi",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postpublish": "git tag -a ${npm_package_version} -m 'Appium Deploy' && git push origin ${npm_package_version}"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "fs": "0.0.1-security",
    "image-size": "^1.0.2",
    "xml2js": "^0.4.23"
  }
}
